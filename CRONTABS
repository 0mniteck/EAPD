# File: /etc/crontabs/root
0 */24 * * * /etc/init.d/eapdd stop && echo "Scheduled Reboot at $(date -u)" >> /root/eapd.log && reboot
*/24 * * * * echo "Scheduled EAPDD run at $(date -u)" >> /root/eapd.log && /etc/init.d/eapdd start

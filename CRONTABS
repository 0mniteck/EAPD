#File: /etc/crontabs/root
0 */12 * * * /etc/init.d/eapd stop && echo "Scheduled Reboot at $(date -u)" >> /root/EvilAPDefender.log && reboot
*/20 * * * * echo "Scheduled EAPD run at $(date -u)" >> /root/EvilAPDefender.log && /etc/init.d/eapd start

#File: /etc/crontabs/root
0 */23 * * * /etc/init.d/eapd stop && echo "Scheduled Reboot at $(date)" >> /root/EvilAPDefender.log && reboot
*/24 * * * * echo "Scheduled EAPD run at $(date)" >> /root/EvilAPDefender.log && /etc/init.d/eapd start
